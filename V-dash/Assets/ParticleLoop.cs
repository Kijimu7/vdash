using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using static DigitalRuby.PyroParticles.FireBaseScript;

public class ParticleLoop : MonoBehaviour
{
    

    // Start is called before the first frame update
    void Start()
    {
        DigitalRuby.PyroParticles.FireBaseScript instance = new DigitalRuby.PyroParticles.FireBaseScript();
        for (int i = 0; i < 10; i++)
        {
            
        }
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
